# CrewAI in Tardygrada

Your 153,245 lines of Python, 2,169 files — in 53 verified instructions.

## What This Is

[Tardygrada](https://github.com/fabio-rovai/tardygrada) is a formally verified agent programming language. 194KB binary. Zero dependencies. Pure C.

This file (`crewai.tardy`) maps your entire agent/task/tool topology into Tardygrada's verified execution model:

- **4 agents** become `receive()` slots with grounded verification
- **35 tools** become `exec()` shell pipelines with output capture
- **6 tasks** become verified claims that go through an 8-layer pipeline
- **Sequential orchestration** becomes `coordinate {...} consensus(ProofWeight)`

## The Difference

| | CrewAI | Tardygrada |
|---|---|---|
| Size | 153,245 lines | 53 instructions |
| Dependencies | 30+ Python packages | Zero |
| Verification | None | 8-layer pipeline + BFT consensus |
| Provenance | None | ed25519 + SHA-256 on every value |
| Agent trust | Implicit | Cryptographic (mprotect + replicas) |

## How to Run

```bash
# Install Tardygrada (194KB, builds in <1 second)
git clone https://github.com/fabio-rovai/tardygrada && cd tardygrada && make

# Run the terraform'd CrewAI
./tardygrada serve tardygrada/crewai.tardy
```

## Generated By

```bash
tardy terraform /path/to/crewAI
```

Not a criticism of CrewAI. A proof that a different paradigm exists.
